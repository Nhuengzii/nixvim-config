{
    plugins = {
        presence-nvim = {
            enable = true;
	    mainImage = "file";
        };
    };
    plugins.neo-tree = {
      enable = true;
    };

    plugins.telescope = {
      enable = true;
    };
}
